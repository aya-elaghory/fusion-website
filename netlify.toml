[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Route /api/* to the Netlify function proxy
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api"
  status = 200
